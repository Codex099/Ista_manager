import 'package:flutter/material.dart';

class optique extends StatelessWidget {
  const optique({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Optique visuel"),),
    );
  }
}